#
# Travis CI job configuration for the Yawg project.
#
# https://travis-ci.org/
#

language: java

jdk:
  - oraclejdk8

script:
  - ./devtools/bin/build-create-bundle --with-unit-tests

cache:
  directories:
    - $HOME/.m2

addons:
  apt:
    packages:
      - xsltproc
